# Scope UI cost

The scope graticule, offscreen marker indicator and editable numeric controls are browser-only changes using existing dependencies. Incremental hardware, component and software-package cost: **$0**.
